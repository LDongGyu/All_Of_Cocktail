<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.leedonggyu.mobile_app_develop">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".CockWhatActivity" />
        <activity android:name=".CockWhatDetailActivity" />
        <activity android:name=".info.CockInfoActivity" />
        <activity android:name=".info.CockInfoListActivity" />
        <activity android:name=".info.CockInfoSiteActivity" />
        <activity android:name=".Manage.ManagementActivity" />
        <activity android:name=".Manage.MemoActivity" />
        <activity android:name=".showMemoActivity" android:theme="@android:style/Theme.Dialog"></activity>

        <receiver
            android:name=".Broadcast"
            android:enabled="true"
            android:exported="true" />

    </application>

</manifest>